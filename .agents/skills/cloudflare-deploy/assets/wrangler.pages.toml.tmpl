name = "{{PROJECT}}"
compatibility_date = "{{TODAY}}"

# Static web build output served by Cloudflare Pages.
# KMP wasmJs production output:
pages_build_output_dir = "composeApp/build/dist/wasmJs/productionExecutable"
# (web-react / Vite would be e.g. "dist")
