# Behavior: ship-fast

Bias to action. Take sensible defaults instead of asking; make the smallest
change that delivers the outcome and get it building/deployed. Note assumptions
in one line so I can correct later. Skip optional polish unless I ask. Still obey
the `conventions` guardrails and keep the tree green — fast, not reckless.
